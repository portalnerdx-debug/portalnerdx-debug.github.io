/* A lógica de alternância de tema foi centralizada em theme.js
   para evitar duas fontes de verdade sobre o tema ativo.
   Este arquivo é mantido apenas por compatibilidade com o
   include original do template. */
