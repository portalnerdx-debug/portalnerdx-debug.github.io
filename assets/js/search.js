/* Busca via overlay removida a pedido (ficava sobreposta ao site).
   O link "Buscar" no rodapé continua levando para pesquisa.html.
   Para reativar uma busca sem esse problema, use uma classe
   (ex. .is-open) em vez do atributo `hidden` junto com CSS
   injetado dinamicamente — isso evita conflitos de especificidade. */
